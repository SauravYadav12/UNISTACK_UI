import {
    createBrowserRouter,

} from "react-router-dom";

const route = createBrowserRouter([
    {
        path: "/",
        // element: <Root />,
        children: [

        ],
    },
]);

export { route }
